apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: knative-slack-bolt
  namespace: default
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/min-scale: "1"
    spec:
      containers:
        - image: 093896728566.dkr.ecr.us-east-1.amazonaws.com/knative-slack-bolt:latest
          env:
            - name: SLACK_BOT_TOKEN
              value: "xoxb-"
            - name: SLACK_APP_TOKEN
              value: "xapp-"
